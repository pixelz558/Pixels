{
  "name": "parcel-stripe",
  "version": "1.0.0",
  "description": "Parcel — a shared pixel canvas where each pixel costs 1 EUR, paid via real Stripe Checkout.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "stripe": "^16.9.0"
  }
}
